{
  "name": "denvercriticalmass.github.io",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest",
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.40",
    "prettier": "^3.3.3",
    "tailwindcss": "^3.4.7",
    "vite": "^5.3.5"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "dev": "vite",
    "deploy": "vite build && git add . && git commit -m 'Deploy' && git push && git subtree push --prefix dist origin gh-pages"
  }
}
